#include <stdio.h>

struct Vector2f[4] calc_rotation_vectors(double robot_left_to_right_length,
                                         double robot_front_to_back_length;
                                         struct Vector2f rotation_cord;) {
  struct Vector2f cords[4] = {{0, 0}, {0, 0}, {0, 0}, {0, 0}};
  return cords;
}
